# Backend Environment Variables

# Server Configuration
PORT=3000

# Firebase Configuration
# IMPORTANT: Use Option 1 OR Option 2, not both

# Option 1: Use file path to service account key (RECOMMENDED)
# Save your Firebase service account JSON to a file and set the path here
GOOGLE_APPLICATION_CREDENTIALS=./serviceAccountKey.json

# Option 2: Use service account JSON as a single-line string
# To use this: 1) Delete the GOOGLE_APPLICATION_CREDENTIALS line above
#              2) Uncomment and set FIREBASE_SERVICE_ACCOUNT below
#              3) The JSON must be on ONE LINE with no line breaks
#              4) Wrap the JSON in single quotes to avoid .env parse errors
# FIREBASE_SERVICE_ACCOUNT='{"type":"service_account","project_id":"your-project",...}'

# CV Service URL
CV_SERVICE_URL=http://localhost:5000

# CORS Configuration (comma-separated origins)
CORS_ORIGINS=http://localhost:3001,http://localhost:19006
